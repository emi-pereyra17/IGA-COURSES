# TEMPLATE: Variables de entorno para Docker
# Copia este archivo a .env.docker y reemplaza los valores con los tuyos
# El archivo .env.docker (que crees a partir de este) debe estar en .gitignore

# CodeIgniter (backend PHP)
CI_ENVIRONMENT=production

# Base de Datos MySQL
DB_HOST=db
DB_PORT=3306
DB_USERNAME=iga_user
DB_PASSWORD=iga_password
DB_DATABASE=iga_courses

# CORS - Frontend autorizado
# En desarrollo: http://localhost:5173
# En producción: https://tu-dominio.com
CORS_ORIGIN=http://localhost:5173
